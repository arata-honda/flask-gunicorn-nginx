version: '3'
services:
  web:
    build: ./web
    container_name: web
    expose:
      - 5000
    networks:
      - nginx_network

  nginx:
    build: ./nginx
    ports:
      - 5000:80
    depends_on:
      - web
    networks:
      - nginx_network

networks:
  nginx_network:
    driver: bridge
